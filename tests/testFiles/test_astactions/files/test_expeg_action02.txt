ExPeg >> RootDefinition >> DefinitionIdentifier > Identifier {
    root.name = target.get_str({Spacing : ""});
}
Definition {
    root.number = index(root);
    root.identifiers = [];
    root.root_exp = [];
}
Definition >> DefinitionIdentifier {
    root.definitionidentifier = target;
}
Definition >> DefinitionExpression >> Identifier {
    root.identifiers.append("<list>" + target.get_str({Spacing:""}) + "</list>");
    target.number = index(target);
}
ExPeg >> Definition {
    target.root_exp.append("<root>" + root.name + "</root>");
    target.plus_num = 1000000 + target.number;
}
